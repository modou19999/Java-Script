/** @format */

// Declara dos variables de tipo string. Imprime por consola el resultado de concatenarlas.

{
  const userName = "GymAesthe";
  let marca = " k.bmt";
  console.log(userName + marca); // esto es concatenarlas
}

{
  const gym = "fitnes Park";
  const marcaPersonal = "Gym Aesthe";
  console.log(`${gym} and ${marcaPersonal}`);
}
