/** @format */

// Declara una variable de las dos formas posibles, e imprime por consola los dos valores.

{
  // v1
  const name = "Lucia";
  console.log(name);

  // v2

  const userName = "Fall";
  console.log(userName);

  // v1
  let age = 22;
  console.log(age);
}

{
  const job = "Developer";
  let address = " Strit francisco";
  let person = job + address;
  console.log(person);
}

{
  const job = "Developer";
  let address = "Strit francisco";
  let person = `${job} ${address}`;
  console.log(person);
}
